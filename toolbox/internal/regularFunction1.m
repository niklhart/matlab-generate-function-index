function [a,b] = regularFunction1(c,d,e)
    % REGULARFUNCTION1 A function with help inside the function body
    % Function help continues here, but only the first line is parsed.

    a = c + d;
    b = e;

end

