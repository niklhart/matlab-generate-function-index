% REGULARFUNCTION2 A function with help outside the function body
% Function help continues here, but only the first line is parsed.
function [a,b] = regularFunction2(c,d,e)

    a = c + d;
    b = e;

end

